export default function InitiativeDetailsPage({ params }: { params: { id: string } }): React.JSX.Element {
    return (
        <div className="p-4">
            <p>Initiative details page</p>
        </div>
    );
}
